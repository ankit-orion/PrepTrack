generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Progress {
  userId    String   @id
  checks    Json
  notes     Json?    @default("[]")
  updatedAt DateTime @updatedAt

  @@map("progress")
}
